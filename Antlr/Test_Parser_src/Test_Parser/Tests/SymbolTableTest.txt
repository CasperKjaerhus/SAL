include testLib;

function main(number n) returns void begin

	for(i: num...10) begin
		num = num + i;
	end
	
	foreach(numb in numArray) begin
		if(numb == 2*n) then begin
			string str = "This is a string";
		end
	end
	
	while(num > 10) begin
		for(i: num...100, step numArray) begin
			number testNumber = i;
			funcTest('c', 4, "string");
		end
	end
end

function funcTest(char c, number num1, string str) returns string begin
	number newNum = num1 + num;
	char newC = c;
	if(newC == 'c') then begin
		string newStr = str;
	end

	newC = 'f';

	return "21";
end

number num = 2;
number numArray[5] = {1,2,3,4,5};